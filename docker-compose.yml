services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-kontakt}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-kontakt}
      POSTGRES_DB: ${POSTGRES_DB:-kontakt}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-kontakt} -d ${POSTGRES_DB:-kontakt}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    profiles:
      - infra
      - prod
    networks:
      - internal

  keycloak:
    image: quay.io/keycloak/keycloak:26.2
    container_name: kontakt-keycloak
    restart: unless-stopped
    command: start-dev
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HTTP_PORT: 8080
    ports:
      - "${KEYCLOAK_PORT:-4080}:8080"
    volumes:
      - keycloak_data:/opt/keycloak/data
    profiles:
      - infra
      - prod
    networks:
      - internal

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-kontakt}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-kontakt123}
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    profiles:
      - infra
    networks:
      - internal

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 kontakt kontakt123 &&
      mc mb --ignore-existing local/kontakt &&
      mc anonymous set download local/kontakt &&
      exit 0
      "
    profiles:
      - infra
    networks:
      - internal

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    entrypoint: ["sh", "./apps/api/entrypoint.sh"]
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-kontakt}:${POSTGRES_PASSWORD:-kontakt}@postgres:5432/${POSTGRES_DB:-kontakt}?schema=public
      NODE_ENV: production
      PORT: 3000
      OIDC_ISSUER: http://keycloak:8080/realms/kontakt
      OIDC_CALLBACK_URL: http://localhost/api/auth/callback
    volumes:
      - uploads:/app/uploads
    profiles:
      - prod
    networks:
      - internal

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    profiles:
      - prod
    networks:
      - internal

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
      web:
        condition: service_started
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    profiles:
      - prod
    networks:
      - internal

volumes:
  pgdata:
  keycloak_data:
  minio_data:
  uploads:
  caddy_data:
  caddy_config:

networks:
  internal:
    driver: bridge
