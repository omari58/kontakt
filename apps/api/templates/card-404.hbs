<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} | Kontakt</title>
  <meta name="robots" content="noindex, nofollow">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      background-color: #f5f5f5;
      color: #1a1a1a;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 24px 16px;
    }

    .error-container {
      text-align: center;
      max-width: 400px;
    }

    .error-code {
      font-size: 72px;
      font-weight: 700;
      color: #ccc;
      line-height: 1;
    }

    .error-message {
      font-size: 20px;
      font-weight: 600;
      margin-top: 16px;
    }

    .error-description {
      font-size: 14px;
      color: #666;
      margin-top: 8px;
      line-height: 1.5;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <p class="error-code">404</p>
    <h1 class="error-message">{{title}}</h1>
    <p class="error-description">This card may have been removed or the link is incorrect.</p>
  </div>
</body>
</html>
